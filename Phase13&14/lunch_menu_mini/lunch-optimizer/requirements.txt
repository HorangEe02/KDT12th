# =============================================================================
# Phase11 lunch-optimizer — Python 의존성
# =============================================================================
# 설치: pip install -r requirements.txt
# Python 3.10+

# --- Core ---
requests==2.32.3
pandas==2.2.3
sqlalchemy==2.0.36
python-dotenv==1.0.1
python-dateutil==2.9.0

# --- API 서버 ---
fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1

# --- 스케줄러 ---
apscheduler==3.11.0

# --- Nutrition 퍼지 매칭 (선택적 — 없으면 difflib fallback) ---
thefuzz==0.22.1
python-Levenshtein==0.26.1

# --- 인증 (Phase 13&14: JWT + bcrypt + RBAC) ---
# bcrypt 4.x 직접 사용 (passlib 1.7.4 는 bcrypt 4.x 와 호환 깨짐)
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
email-validator==2.2.0
slowapi==0.1.9

# --- 테스트 / 품질 ---
pytest==8.3.4
pytest-cov==5.0.0
ruff==0.8.6
