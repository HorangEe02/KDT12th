# Phase5 main_usa - MySQL 접속 정보
# 실제 값을 채운 .env 파일을 만들고(절대 커밋 금지), 아래 변수를 환경에 주입하세요.
#   export $(grep -v '^#' .env | xargs)   # 또는 python-dotenv 사용
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
